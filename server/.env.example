# Flask Core Configuration
FLASK_APP=app.py
FLASK_DEBUG=true

# Security Secrets (Generate using: python -c "import secrets; print(secrets.token_hex(32))")
SECRET_KEY=your_secret_key_here
JWT_SECRET_KEY=your_jwt_secret_key_here

# Database Configuration (Local default SQLite or Production PostgreSQL)
DATABASE_URL=sqlite:///../instance/acreage.db

# Comma-separated CORS allowed origins
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Safaricom M-Pesa Credentials
MPESA_ENV=sandbox
MPESA_SHORTCODE=174379
MPESA_CONSUMER_KEY=your_mpesa_consumer_key
MPESA_CONSUMER_SECRET=your_mpesa_consumer_secret
MPESA_PASSKEY=your_mpesa_passkey
MPESA_CALLBACK_URL=https://your-domain-or-ngrok.ngrok-free.app/api/payouts/callback

# Account email and security tokens
FRONTEND_URL=http://localhost:5173
SECURITY_TOKEN_EXPIRES_MINUTES=30
# Keep false only for local development without SMTP. Production should be true.
EMAIL_VERIFICATION_REQUIRED=false
MAIL_SERVER=smtp.example.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=your_smtp_username
MAIL_PASSWORD=your_smtp_password
MAIL_DEFAULT_SENDER=no-reply@example.com
LOG_LEVEL=INFO

# Rate limiting: use a durable Redis URI in production; memory:// is local-only.
RATELIMIT_STORAGE_URI=memory://
